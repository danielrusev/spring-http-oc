apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/danielrusev/spring-http-oc:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/danielrusev/spring-http-oc:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: spring-http-oc
        chartPath: charts/spring-http-oc
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/danielrusev/spring-http-oc"
          image.tag: "{{.DIGEST_HEX}}"
